kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 80 # Porta do NGINX dentro do contêiner
    hostPort: 30080   # Porta exposta na sua máquina
    protocol: TCP

